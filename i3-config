# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

new_window 1pixel
# for_window [instance="caja"] floating enable, resize set 800 640, move scratchpad
for_window [title=".*Tor Browser"] layout tabbed
for_window [title=".*Visual Studio Code"] layout tabbed
for_window [class=".*"] border pixel 0

# Use this line to bind $mod to Windows key.
# set $mod Mod4

# Use this line to bind $mod to Alt key.
set $mod Mod1

# Binds caps-lock to escape
exec "setxkbmap -option caps:escape"

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# disable switching windows on mouse scroll
bindsym button4 nop
bindsym button5 nop

bindsym $mod+Shift+Control+a mark "scratch_a", move scratchpad
bindsym $mod+Shift+Control+b mark "scratch_b", move scratchpad
bindsym $mod+Shift+Control+c mark "scratch_c", move scratchpad
bindsym $mod+Shift+Control+d mark "scratch_d", move scratchpad
bindsym $mod+Shift+Control+e mark "scratch_e", move scratchpad
bindsym $mod+Shift+Control+f mark "scratch_f", move scratchpad
bindsym $mod+Shift+Control+g mark "scratch_g", move scratchpad
bindsym $mod+Shift+Control+h mark "scratch_h", move scratchpad
bindsym $mod+Shift+Control+i mark "scratch_i", move scratchpad
bindsym $mod+Shift+Control+j mark "scratch_j", move scratchpad
bindsym $mod+Shift+Control+k mark "scratch_k", move scratchpad
bindsym $mod+Shift+Control+l mark "scratch_l", move scratchpad
bindsym $mod+Shift+Control+m mark "scratch_m", move scratchpad
bindsym $mod+Shift+Control+n mark "scratch_n", move scratchpad
bindsym $mod+Shift+Control+o mark "scratch_o", move scratchpad
bindsym $mod+Shift+Control+p mark "scratch_p", move scratchpad
bindsym $mod+Shift+Control+q mark "scratch_q", move scratchpad
bindsym $mod+Shift+Control+r mark "scratch_r", move scratchpad
bindsym $mod+Shift+Control+s mark "scratch_s", move scratchpad
bindsym $mod+Shift+Control+t mark "scratch_t", move scratchpad
bindsym $mod+Shift+Control+u mark "scratch_u", move scratchpad
bindsym $mod+Shift+Control+v mark "scratch_v", move scratchpad
bindsym $mod+Shift+Control+w mark "scratch_w", move scratchpad
bindsym $mod+Shift+Control+x mark "scratch_x", move scratchpad
bindsym $mod+Shift+Control+y mark "scratch_y", move scratchpad
bindsym $mod+Shift+Control+z mark "scratch_z", move scratchpad
bindsym $mod+Shift+Control+0 mark "scratch_0", move scratchpad
bindsym $mod+Shift+Control+1 mark "scratch_1", move scratchpad
bindsym $mod+Shift+Control+2 mark "scratch_2", move scratchpad
bindsym $mod+Shift+Control+3 mark "scratch_3", move scratchpad
bindsym $mod+Shift+Control+4 mark "scratch_4", move scratchpad
bindsym $mod+Shift+Control+5 mark "scratch_5", move scratchpad
bindsym $mod+Shift+Control+6 mark "scratch_6", move scratchpad
bindsym $mod+Shift+Control+7 mark "scratch_7", move scratchpad
bindsym $mod+Shift+Control+8 mark "scratch_8", move scratchpad
bindsym $mod+Shift+Control+9 mark "scratch_9", move scratchpad

bindsym $mod+Control+a [con_mark="scratch_a"] scratchpad show
bindsym $mod+Control+b [con_mark="scratch_b"] scratchpad show
bindsym $mod+Control+c [con_mark="scratch_c"] scratchpad show
bindsym $mod+Control+d [con_mark="scratch_d"] scratchpad show
bindsym $mod+Control+e [con_mark="scratch_e"] scratchpad show
bindsym $mod+Control+f [con_mark="scratch_f"] scratchpad show
bindsym $mod+Control+g [con_mark="scratch_g"] scratchpad show
bindsym $mod+Control+h [con_mark="scratch_h"] scratchpad show
bindsym $mod+Control+i [con_mark="scratch_i"] scratchpad show
bindsym $mod+Control+j [con_mark="scratch_j"] scratchpad show
bindsym $mod+Control+k [con_mark="scratch_k"] scratchpad show
bindsym $mod+Control+l [con_mark="scratch_l"] scratchpad show
bindsym $mod+Control+m [con_mark="scratch_m"] scratchpad show
bindsym $mod+Control+n [con_mark="scratch_n"] scratchpad show
bindsym $mod+Control+o [con_mark="scratch_o"] scratchpad show
bindsym $mod+Control+p [con_mark="scratch_p"] scratchpad show
bindsym $mod+Control+q [con_mark="scratch_q"] scratchpad show
bindsym $mod+Control+r [con_mark="scratch_r"] scratchpad show
bindsym $mod+Control+s [con_mark="scratch_s"] scratchpad show
bindsym $mod+Control+t [con_mark="scratch_t"] scratchpad show
bindsym $mod+Control+u [con_mark="scratch_u"] scratchpad show
bindsym $mod+Control+v [con_mark="scratch_v"] scratchpad show
bindsym $mod+Control+w [con_mark="scratch_w"] scratchpad show
bindsym $mod+Control+x [con_mark="scratch_x"] scratchpad show
bindsym $mod+Control+y [con_mark="scratch_y"] scratchpad show
bindsym $mod+Control+z [con_mark="scratch_z"] scratchpad show
bindsym $mod+Control+0 [con_mark="scratch_0"] scratchpad show
bindsym $mod+Control+1 [con_mark="scratch_1"] scratchpad show
bindsym $mod+Control+2 [con_mark="scratch_2"] scratchpad show
bindsym $mod+Control+3 [con_mark="scratch_3"] scratchpad show
bindsym $mod+Control+4 [con_mark="scratch_4"] scratchpad show
bindsym $mod+Control+5 [con_mark="scratch_5"] scratchpad show
bindsym $mod+Control+6 [con_mark="scratch_6"] scratchpad show
bindsym $mod+Control+7 [con_mark="scratch_7"] scratchpad show
bindsym $mod+Control+8 [con_mark="scratch_8"] scratchpad show
bindsym $mod+Control+9 [con_mark="scratch_9"] scratchpad show

bindsym $mod+x scratchpad show

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+f exec dmenu_run

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# Pulse Audio controls
# NOTE(DarinM223): 1 is the sink number found with `pactl list sinks`
# bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 1 +5% #increase sound volume
# bindsym XF86AudioLowerVolume exec pactl set-sink-volume 1 -5% #decrease sound volume
# bindsym XF86AudioMute exec pactl set-sink-mute 1 toggle # mute sound

# mod+; temporarily turns on borders on focused window
bindcode $mod+47 border pixel 4
bindcode $mod+47 --release border pixel 0

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+m split h

# split in vertical orientation
bindsym $mod+n split v

# enter fullscreen mode for the focused container
bindsym $mod+Shift+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+c layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status
        tray_output primary
        tray_padding 0
        wheel_up_cmd nop
        wheel_down_cmd nop
}

focus_follows_mouse no

# Starts pa-applet
# NOTE(DarinM223): first install pa-applet by following the instructions
# from https://github.com/fernandotcl/pa-applet
exec --no-startup-id pa-applet
# NOTE(DarinM223): first install xbindkeys, xsel, and xdotool with apt-get
# Don't forget to create a .xbindkeysrc file to turn off middle mouse click paste!
exec --no-startup-id xbindkeys
exec --no-startup-id redshift-gtk
exec --no-startup-id feh --bg-scale ~/.background-image
